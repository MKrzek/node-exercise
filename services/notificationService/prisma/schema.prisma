generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model EmailLog {
  id           String   @id @default(uuid())
  recipient    String
  subject      String
  body         String
  status       String
  sentAt       DateTime @default(now())
  errorMessage String?
}